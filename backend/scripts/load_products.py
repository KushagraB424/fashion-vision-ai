def load_products():
    """
    Placeholder function.
    Previously used for FAISS product loading.
    Now shopping links are generated dynamically.
    """
    print("Product loader disabled (dynamic shopping links enabled).")